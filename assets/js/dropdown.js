const Dropdown={toggle(id){const x=document.getElementById(id);if(!x)return;this.closeAll(x);x.classList.toggle('is-open');x.setAttribute('aria-hidden',x.classList.contains('is-open')?'false':'true')},closeAll(except){document.querySelectorAll('.dropdown-panel.is-open').forEach(x=>{if(x!==except)x.classList.remove('is-open')})}};document.addEventListener('click',e=>{if(!e.target.closest('.dropdown-wrap'))Dropdown.closeAll()});
